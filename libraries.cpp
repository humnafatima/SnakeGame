#include <iostream>   // input/output
#include <vector>     // dynamic array (obstacles)
#include <list>       // snake body (linked list)
#include <cstdlib>    // rand()
#include <conio.h>    // _kbhit(), _getch() → keyboard input
#include <windows.h>  // Sleep(), system("cls")